import {createTheme} from "@material-ui/core";

export const theme = createTheme({
  palette: {
    primary: {
      main: '#00a0fc',
      contrastText: '#ffffff',
    },
    secondary: {
      main: '#00f38e',
      contrastText: '#ffffff'
    },
  },
});
