import {createTheme} from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: '#4F6367',
      contrastText: '#ffffff',
    },
    secondary: {
      main: '#7A9E9F',
      contrastText: '#ffffff'
    },
  },
});
